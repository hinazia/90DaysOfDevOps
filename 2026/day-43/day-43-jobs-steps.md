
- What needs: and outputs: do in your own words
  - needs: creates the dependency of one job onto another
  - outputs: shows the output values on the job level so that other jobs can access the values and use them
 
 
